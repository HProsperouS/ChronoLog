# Backend API URL
# Uncomment the line below ONLY when the backend is running (cd backend && npm run dev).
# While it's commented out, the frontend uses the mock JSON files in src/data/.
VITE_API_URL=http://localhost:3001 