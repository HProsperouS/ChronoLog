# ChronoLog
For high-intensity individuals who struggle with fragmented focus, ChronoLog aims to be THE zero-friction, context-aware, time tracking application that provides effortless and trustworthy insights ​

## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  